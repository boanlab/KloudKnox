# SPDX-License-Identifier: Apache-2.0
# Valid KloudKnoxPolicy used by the k8s-live operator check.
apiVersion: security.boanlab.com/v1
kind: KloudKnoxPolicy
metadata:
  name: k8slive-valid-test
spec:
  selector:
    container: ubuntu-1
  process:
    - path: /bin/sleep
  action: Block
