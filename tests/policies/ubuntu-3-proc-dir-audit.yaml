apiVersion: security.boanlab.com/v1
kind: KloudKnoxPolicy
metadata:
  name: ubuntu-3-proc-dir-audit
spec:
  selector:
    container: ubuntu-3
  process:
    - dir: /sbin/
    - dir: /usr/sbin/
  action: Audit

# test
# $ route                          # /sbin/route (audit)
# ... route output ...
